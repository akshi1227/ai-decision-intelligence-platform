kind = "web"
previewPath = "/"
title = "Decision IQ"
version = "1.0.0"
id = "artifacts/decision-iq"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24353

[services.development]
run = "pnpm --filter @workspace/decision-iq run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/decision-iq", "run", "build" ]
publicDir = "artifacts/decision-iq/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24353"
BASE_PATH = "/"
